server:
  host: "0.0.0.0"
  port: 8080

auth:
  enabled: true
  api_keys:
    - "dev-gateway-key-1"

metrics:
  enabled: true

rate_limit:
  enabled: true
  requests_per_minute: 60

cache:
  enabled: false
  ttl_seconds: 60
  max_entries: 128
