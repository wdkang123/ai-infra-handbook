# Starter File Run 03

## Mode

微任务修订模式（直接就地修订原蓝图文件）

## Goal

收掉 starter file 阶段剩余的 2 个尾巴问题，使 `T1001` 和 `T1005` 也能进入 `accepted/`。

## Run Order

1. `tasks/ready/T1021-inference-stream-syntax-fix.md`
2. `tasks/ready/T1025-root-smoke-model-pass-through-fix.md`

## Notes

- 本轮不要新建平行文件名，直接覆盖 `tasks/review-pending/` 下的原文件。
- 每个任务完成后都要明确列出实际修改过的绝对路径。
- 只修 review note 指定的问题，不重写整包。
